我在src/utils/jsonl.rs下写了一个库可以实现以下功能
1. 给一个文件末尾append一个json对象
2. 读文件末尾最后几个json对象

我需要你实现以下功能，对于appleby来说，需要
1. 将当前对话上下文实时append到文件中，以留痕。
2. 启动时自动检查有没有之前的上下文文件如果有上下文，如果有的话就加载最后20个对话内容
3. 给appleby加一个新命令，可以在启动的时候显式声明不加载之前的上下文。具体做法是将之前的文件重命名为日期结尾，然后重写创建文件

