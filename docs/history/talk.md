我在src/utils/jsonl.rs下写了一个库可以实现以下功能
1. 给一个文件末尾append一个json对象
2. 读文件末尾最后几个json对象

我需要你实现以下功能，对于appleby来说，需要
1. 将当前对话上下文实时append到文件中，以留痕。
2. 启动时自动检查有没有之前的上下文文件如果有上下文，如果有的话就加载最后20个对话内容
3. 给appleby加一个新命令，可以在启动的时候显式声明不加载之前的上下文。具体做法是将之前的文件重命名为日期结尾，然后重写创建文件

整理下代码，LoopState的new不要从全局变量读文件path，也不要通过load_previous_context传bool来加载，抽象一个文件上下文管理机制。在appleby.rs中通过传参数控制是否加载上下文。总体核心目标是，除了appleby.rs  以外其他库尽量不依赖全局变量、文件等。做到bin和lib分离

我觉得这个文档写的不行，我觉得要分为以下几个部分，第一，我们在claude.md 中已经声明了我们需要那些一致性，这些一致性是否合理。第二。在新增了t  
  ui这个复杂度之后，我们应该添加那些一致性规范。然后我们要跟Claude.md里面的一致性规范做一个排序。在代码可能不满足所有规范时，我们优先满足排名  
  靠前的。第三，当前代码具体不满足那些规范，所以我们要怎么改，以满足规范。第四
  我们不能盲目的说这样时“优化”，不要有模糊的“优化”这个说法，除非当前代码明显是错的。所有的优化目标都只有靠近规范